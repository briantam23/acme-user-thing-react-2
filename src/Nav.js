import React, { Component } from 'react';
import { connect } from 'react-redux';

class Nav extends Component {
    render() {
        return(
            <h1>Acme-User-Thing-React-2</h1>
        )
    }
}

export default Nav;